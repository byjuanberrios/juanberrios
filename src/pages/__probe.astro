---
import { getCollection } from "astro:content";
const posts = await getCollection("posts");
const p = posts[0];
const info = "PROBEMARK keys=" + Object.keys(p).join(",") + " bodyType=" + typeof p.body + " bodyLen=" + (p.body ? p.body.length : -1);
---
<p>{info}</p>
